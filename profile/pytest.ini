[pytest]
testpaths = tests
markers =
    slow: re-parses full traces (deselect with '-m "not slow"')
    known_broken: models with known parsing issues (auto-xfail)
addopts = -m "not slow" --tb=short
